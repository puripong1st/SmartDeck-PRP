# Shared UI Package

This package is reserved for shared UI components if the Local Web UI grows enough to justify a package boundary.

Keep this empty until duplication or reuse makes it worthwhile.

