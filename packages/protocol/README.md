# SmartDeck Protocol Package

This package will contain shared SmartDeck Protocol schemas and TypeScript types.

MVP responsibilities:

- Message envelope definitions
- Profile, page, button, action, widget, asset, and firmware status schemas
- Validation helpers for Local Web UI and Local Bridge
- Protocol version constants

