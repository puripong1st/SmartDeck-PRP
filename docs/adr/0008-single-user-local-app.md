# Single-user local app

The MVP will be a single-user local app with no login or registration. Local records will still use stable IDs and timestamps so cloud accounts, sync, teams, and enterprise identity can be introduced later without forcing authentication into the first release.
