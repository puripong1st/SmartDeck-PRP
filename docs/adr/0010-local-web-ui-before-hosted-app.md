# Local web UI before hosted app

The MVP will ship as a local web UI that works with the local bridge instead of a hosted public web app. This fits the local-first product shape, avoids early browser security and deployment complexity around hosted pages calling localhost, and keeps cloud dashboard concerns deferred until accounts and sync exist.
