# PNG and JPG assets first

The MVP will support PNG and JPG assets first, while GIF animation and SVG rendering are deferred. Static raster assets cover the initial button and profile use cases with a simpler firmware and asset pipeline, avoiding early memory, decoding, and performance complexity on the device.
