# MVP deliverables over full platform deliverables

The first delivery will produce MVP deliverables instead of the full platform deliverable list. The original product vision remains broader, but the first release will focus on local-first architecture, firmware, local bridge, local web UI, profiles, actions, widgets, device protocol, local APIs, testing, security, and local deployment.
