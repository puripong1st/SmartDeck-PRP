# Defer plugins and marketplace

The MVP will use a built-in action registry instead of shipping a plugin system or marketplace. This keeps third-party code execution, permissions, asset distribution, reviews, and monetization out of the first release while preserving a clear path to external extensions later.
