# Manual USB firmware update before OTA

The MVP will support manual firmware updates over USB and defer OTA updates. USB update keeps the first firmware release path controllable and recoverable, while OTA, signed releases, cloud channels, staged rollout, and fleet deployment are deferred until the core device and bridge flow is stable.
