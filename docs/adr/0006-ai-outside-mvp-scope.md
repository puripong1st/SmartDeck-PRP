# AI outside MVP scope

The MVP will not include AI features or AI action modeling. AI-assisted commands, provider integrations, prompt libraries, voice input, and local model support are explicitly outside the first release so the project can focus on the hardware control loop, web app, local bridge, profiles, actions, and widgets.
