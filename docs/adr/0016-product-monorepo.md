# Product monorepo

SmartDeck Pro MVP will use a product monorepo containing the local web UI, local bridge, firmware, shared protocol package, and documentation. The components run in different environments, but they form one hardware-software product and need shared schemas, coordinated versioning, and local release packaging.
