# Enterprise features outside MVP scope

The MVP will not include enterprise features such as fleet management, team workspaces, role-based access control, audit logs, remote deployment, or organization roles. Those capabilities depend on cloud identity, policies, multi-device management, retention, and deployment infrastructure, so the first release will only keep local device status and stable record identifiers.
