# SmartDeck Protocol

SmartDeck Pro MVP will use a versioned JSON message model called SmartDeck Protocol between the local web UI, local bridge, and device. A shared protocol avoids ad hoc integration between components, gives the firmware a constrained message subset, and leaves room for cloud or mobile clients to reuse the same product language later.
