# Localhost-only bridge API

The local bridge API will bind only to localhost and require bridge pairing with a random token. The bridge can launch apps, send input, inject text, read local state, and sync devices, so it must not expose a public LAN or internet API in the MVP.
