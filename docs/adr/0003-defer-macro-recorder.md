# Defer macro recorder

The MVP will include a manual macro builder but will defer a true mouse and keyboard macro recorder. Recording real operating system input adds security, permission, antivirus, and reliability risks, while a manual builder still proves multi-step actions without expanding the local bridge scope too early.
