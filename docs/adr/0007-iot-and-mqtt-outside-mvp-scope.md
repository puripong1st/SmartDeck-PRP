# IoT and MQTT outside MVP scope

The MVP will not include IoT integrations or MQTT commands. HTTP request actions remain in scope as a basic integration mechanism, while MQTT-specific broker configuration, credentials, topic conventions, reconnect behavior, Home Assistant compatibility, and device-control permissions are deferred until after the core local product is stable.
